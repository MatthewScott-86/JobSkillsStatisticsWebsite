worker: pip install xlrd && pip install geopandas && pip install beautifulsoup4 && pip install plotly && pip install pandas 
web: pip install waitress && waitress-serve --port=$PORT EmploymentSkillsStatisticsProject.wsgi:application