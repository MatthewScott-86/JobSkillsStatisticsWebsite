web: pip install matplotlib && pip install requests && pip install xlrd && pip install pyshp && pip install shapely && pip install geopandas && pip install beautifulsoup4 && pip install plotly && pip install pandas && pip install waitress && waitress-serve --port=$PORT EmploymentSkillsStatisticsProject.wsgi:application