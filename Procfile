web: pip install matplotlib && pip install requests && pip install xlrd && pip install pyshp && pip install shapely && pip install beautifulsoup4 && pippip install html-testRunner && pip install selenium && pip install plotly && pip install pandas && pip install waitress && waitress-serve --port=$PORT EmploymentSkillsStatisticsProject.wsgi:application